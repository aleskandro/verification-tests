apiVersion: v1
kind: Pod
metadata:
  generateName: network-pod-
  labels:
          name: network-pod
spec:
  containers:
  - name: network-pod
    image: quay.io/aleskandrox/hello-pod:centos-8080
    securityContext:
      capabilities:
        add: ["NET_ADMIN"]
  hostNetwork: true
  nodeName: node-name
